// This file is no longer needed - commands are now modular
// Individual commands are located in the commands/ directory
// Each command is a separate file with its own data and execute function

module.exports = {
    // This file can be deleted or used for command utilities
    // All commands are now automatically loaded from the commands/ directory
};